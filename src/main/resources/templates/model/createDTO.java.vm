package ${package.createDTO};
#if($createDTOFramePkg.size()>0)

#end
#foreach($pkg in ${createDTOFramePkg})
import ${pkg};
#end
#if($createDTOJavaPkg.size()>0)

#end
#foreach($pkg in ${createDTOJavaPkg})
import ${pkg};
#end

/**
 * $!{table.comment}-新增DTO
 *
 * @author ${author}
#if(${docLink})
 * @see ${class.entity}
 * @see ${class.serviceImpl}
#end
 * @since ${date}
 */
#if(${lombok})
@Data
    #if(${chainModel})
@Accessors(chain = true)
    #end
#end
#if(${swagger3})
@Schema(description = "$!{table.comment}-新增DTO")
#elseif(${swagger2})
@ApiModel(description = "$!{table.comment}-新增DTO")
#end
public class ${createDTO}#if(${serializableUID}) implements Serializable#end {
#if(${serializableUID})

    #if(${serializableAnnotation})
    @Serial
    #end
    private static final long serialVersionUID = 1L;
#end
## ----------  BEGIN 字段循环遍历  ----------
#foreach($field in ${table.fields})
#if(${field.keyFlag})
#elseif(${field.versionField})
#elseif(${field.logicDeleteField})
#elseif(${field.fill} && (${field.fill}=="INSERT" || ${field.fill}=="INSERT_UPDATE"))
#elseif(${editExcludeColumns.contains(${field.columnName})})
#else

    #if("$!field.comment" != "")
        #if(${swagger3})
    @Schema(description = "${field.comment}")
        #elseif(${swagger2})
    @ApiModelProperty("${field.comment}")
        #else
    /**
     * ${field.comment}
     */
        #end
    #end
    #if(${validated})## 参数校验
        #if(!${field.metaInfo.nullable})## 非空
            #if(${field.propertyType} == "String")
    @NotBlank(message = "${field.comment}为必填项")
            #else
    @NotNull(message = "${field.comment}为必填项")
            #end
        #end
        #if(${field.propertyType} == "String")## 字符串长度
    @Size(message = "${field.comment}长度不能超过${field.metaInfo.length}", max = ${field.metaInfo.length})
        #end
    #end
    private ${field.propertyType} ${field.propertyName};
#end
#end
## ----------  END 字段循环遍历  ----------
#if(!${lombok})
#foreach($field in ${table.fields})
#if(${field.keyFlag})
#elseif(${field.versionField})
#elseif(${field.logicDeleteField})
#elseif(${field.fill} && (${field.fill}=="INSERT" || ${field.fill}=="INSERT_UPDATE"))
#elseif(${editExcludeColumns.contains(${field.columnName})})
#else
    #if(${field.propertyType.equals("boolean")})
        #set($getprefix="is")
    #else
        #set($getprefix="get")
    #end

    public ${field.propertyType} ${getprefix}${field.capitalName}() {
        return ${field.propertyName};
    }

    #if(${chainModel})
    public ${createDTO} set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #else
    public void set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #end
        this.${field.propertyName} = ${field.propertyName};
    #if(${chainModel})
        return this;
    #end
    }
#end
#end
#end
}
