package ${package.queryDto};
#if($queryDtoFramePkg.size()>0)

#end
#foreach($pkg in ${queryDtoFramePkg})
import ${pkg};
#end
#if($queryDtoJavaPkg.size()>0)

#end
#foreach($pkg in ${queryDtoJavaPkg})
import ${pkg};
#end

/**
 * $!{table.comment}-查询Dto
 *
 * @author ${docAuthor}
#if(${docLink})
 * @see ${class.entity}
 * @see ${class.mapper}
 * @see ${class.serviceImpl}
 * @since ${docDate}
#end
 */
#if(${lombok})
@Data
    #if(${chainModel})
@Accessors(chain = true)
    #end
    #if(${queryDtoExtendsEntity})
@EqualsAndHashCode(callSuper = true)
    #end
#end
#if(${swagger3})
@Schema(description = "$!{table.comment}-查询Dto")
#elseif(${swagger2})
@ApiModel(description = "$!{table.comment}-查询Dto")
#end
#if(${queryDtoExtendsEntity})
public class ${queryDto} extends ${entity}#if(${serializableUID}) implements Serializable#end {
#else
public class ${queryDto}#if(${serializableUID}) implements Serializable#end {
#end
#if(${serializableUID})

    #if(${serializableAnnotation})
    @Serial
    #end
    private static final long serialVersionUID = 1L;
#end
## ----------  BEGIN 字段循环遍历  ----------
#if(!${queryDtoExtendsEntity})
#foreach($field in ${table.fields})
#if(${field.logicDeleteField})
#else

    #if("$!field.comment" != "")
        #if(${swagger3})
    @Schema(description = "${field.comment}")
        #elseif(${swagger2})
    @ApiModelProperty("${field.comment}")
        #else
    /**
     * ${field.comment}
     */
        #end
    #end
    private ${field.propertyClassSimpleName} ${field.propertyName};
#end
#end
#end
## ----------  END 字段循环遍历  ----------
#foreach($field in ${table.extraFields})

    #if("$!field.comment" != "")
        #if(${swagger3})
    @Schema(description = "${field.comment}")
        #elseif(${swagger2})
    @ApiModelProperty("${field.comment}")
        #else
    /**
     * ${field.comment}
     */
        #end
    #end
    private ${field.propertyClassSimpleName} ${field.propertyName};
#end
## --基础getter/setter start--
#if(!${lombok} && !${queryDtoExtendsEntity})
#foreach($field in ${table.fields})
#if(${field.logicDeleteField})
#else

    #if(${field.propertyClassSimpleName.equals("boolean")})
        #set($getprefix="is")
    #else
        #set($getprefix="get")
    #end
    public ${field.propertyClassSimpleName} ${getprefix}${field.capitalName}() {
        return ${field.propertyName};
    }

    #if(${chainModel})
    public ${queryDto} set${field.capitalName}(${field.propertyClassSimpleName} ${field.propertyName}) {
    #else
    public void set${field.capitalName}(${field.propertyClassSimpleName} ${field.propertyName}) {
    #end
        this.${field.propertyName} = ${field.propertyName};
    #if(${chainModel})
        return this;
    #end
    }
#end
#end
#end
## --基础getter/setter end---
## --额外字段getter/setter start--
#if(!${lombok})
#foreach($field in ${table.extraFields})

    #if(${field.propertyClassSimpleName.equals("boolean")})
        #set($getprefix="is")
    #else
        #set($getprefix="get")
    #end
    public ${field.propertyClassSimpleName} ${getprefix}${field.capitalName}() {
        return ${field.propertyName};
    }

    #if(${chainModel})
    public ${queryDto} set${field.capitalName}(${field.propertyClassSimpleName} ${field.propertyName}) {
    #else
    public void set${field.capitalName}(${field.propertyClassSimpleName} ${field.propertyName}) {
    #end
        this.${field.propertyName} = ${field.propertyName};
    #if(${chainModel})
        return this;
    #end
    }
#end
#end
}
