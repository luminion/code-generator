package ${template.queryDto.packageName};
#if($queryDtoFramePkg.size()>0)

#end
#foreach($pkg in ${queryDtoFramePkg})
import ${pkg};
#end
#if($queryDtoJavaPkg.size()>0)

#end
#foreach($pkg in ${queryDtoJavaPkg})
import ${pkg};
#end

/**
 * $!{table.comment}-查询Dto
 *
 * @author ${docAuthor}
#if(${docLink})
 * @see ${class.entity}
 * @see ${class.mapper}
 * @see ${class.serviceImpl}
 * @since ${docDate}
#end
 */
#if(${enableLombok})
@Data
    #if(${enableChainSetter})
@Accessors(chain = true)
    #end
#end
#if(${enableOpenApi3})
@Schema(description = "$!{table.comment}-查询Dto")
#elseif(${enableOpenApi2})
@ApiModel(description = "$!{table.comment}-查询Dto")
#end
public class ${queryDto}#if(${enableSerializableUID}) implements Serializable#end {
#if(${enableSerializableUID})

    #if(${enableSerializableAnnotation})
    @Serial
    #end
    private static final long serialVersionUID = 1L;
#end
## ----------  BEGIN 字段循环遍历  ----------
#foreach($field in ${table.fields})
#if(${field.logicDeleteField})
#else

    #if("$!field.comment" != "")
        #if(${enableOpenApi3})
    @Schema(description = "${field.comment}")
        #elseif(${enableOpenApi2})
    @ApiModelProperty("${field.comment}")
        #else
    /**
     * ${field.comment}
     */
        #end
    #end
    private ${field.propertyType} ${field.propertyName};
#end
#end
## ----------  END 字段循环遍历  ----------
#foreach($field in ${table.extraFields})

    #if("$!field.comment" != "")
        #if(${enableOpenApi3})
    @Schema(description = "${field.comment}")
        #elseif(${enableOpenApi2})
    @ApiModelProperty("${field.comment}")
        #else
    /**
     * ${field.comment}
     */
        #end
    #end
    private ${field.propertyType} ${field.propertyName};
#end
## --基础getter/setter start--
#foreach($field in ${table.fields})
#if(${field.logicDeleteField})
#else

    #if(${field.propertyType.equals("boolean")})
        #set($getprefix="is")
    #else
        #set($getprefix="get")
    #end
    public ${field.propertyType} ${getprefix}${field.capitalName}() {
        return ${field.propertyName};
    }

    #if(${enableChainSetter})
    public ${queryDto} set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #else
    public void set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #end
        this.${field.propertyName} = ${field.propertyName};
    #if(${enableChainSetter})
        return this;
    #end
    }
#end
#end
## --基础getter/setter end---
## --额外字段getter/setter start--
#if(!${enableLombok})
#foreach($field in ${table.extraFields})

    #if(${field.propertyType.equals("boolean")})
        #set($getprefix="is")
    #else
        #set($getprefix="get")
    #end
    public ${field.propertyType} ${getprefix}${field.capitalName}() {
        return ${field.propertyName};
    }

    #if(${enableChainSetter})
    public ${queryDto} set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #else
    public void set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #end
        this.${field.propertyName} = ${field.propertyName};
    #if(${enableChainSetter})
        return this;
    #end
    }
#end
#end
}
