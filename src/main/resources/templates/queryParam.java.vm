package ${queryParam.packageName};
#if($queryParamFramePkg.size()>0)

#end
#foreach($pkg in ${queryParamFramePkg})
import ${pkg};
#end
#if($queryParamJavaPkg.size()>0)

#end
#foreach($pkg in ${queryParamJavaPkg})
import ${pkg};
#end

/**
 * $!{table.comment}-查询条件对象
 *
 * @author ${author}
#if(${javadocWithSeeTags})
 * @see ${entity.classCanonicalName}
 * @see ${mapper.classCanonicalName}
 * @see ${serviceImpl.classCanonicalName}
#end
#if(${date})
 * @since ${date}
#end
 */
#if(${lombok})
@Getter
@Setter
#end
#if(${lombok} && ${chainSetter})
@Accessors(chain = true)
#end
#if(${lombok} && ${toString})
@ToString
#end
#if(${springDoc})
@Schema(description = "$!{table.comment}-查询条件对象")
#elseif(${swagger})
@ApiModel(description = "$!{table.comment}-查询条件对象")
#end
public class ${queryParam.classSimpleName} #if(${queryParamSuperClass})extends ${queryParamSuperClass} #end#if(${serializable})implements Serializable#end{
#if(${serializable})

    #if(${serializableAnnotation})
    @Serial
    #end
    private static final long serialVersionUID = 1L;
#end
#if(!${queryParamSuperClass})

    #if(${springDoc})
    @Schema(description = "当前页码")
    #elseif(${swagger})
    @ApiModelProperty("当前页码")
    #else
    /**
     * 当前页码
     */
    #end
    private Long ${pageParamName};
    #if(${springDoc})
    @Schema(description = "每页条数")
    #elseif(${swagger})
    @ApiModelProperty("每页条数")
    #else
    /**
     * 每页条数
     */
    #end
    private Long ${sizeParamName};
#end
## ----------  BEGIN 字段循环遍历  ----------
#foreach($field in ${table.fields})
#if(${field.logicDeleteField})
#else

    #if("$!field.comment" != "")
        #if(${springDoc})
    @Schema(description = "${field.comment}")
        #elseif(${swagger})
    @ApiModelProperty("${field.comment}")
        #else
    /**
     * ${field.comment}
     */
        #end
    #end
    private ${field.propertyType} ${field.propertyName};
#end
#end
## ----------  END 字段循环遍历  ----------
#foreach($field in ${table.extraFields})

    #if("$!field.comment" != "")
        #if(${springDoc})
    @Schema(description = "${field.comment}")
        #elseif(${swagger})
    @ApiModelProperty("${field.comment}")
        #else
    /**
     * ${field.comment}
     */
        #end
    #end
    private ${field.propertyType} ${field.propertyName};
#end
## --基础getter/setter start--
#if(!${lombok})
    
    
#foreach($field in ${table.fields})
#if(${field.logicDeleteField})
#else

    #if(${field.propertyType.equals("boolean")})
        #set($getprefix="is")
    #else
        #set($getprefix="get")
    #end
    public ${field.propertyType} ${getprefix}${field.capitalName}() {
        return ${field.propertyName};
    }

    #if(${chainSetter})
    public ${queryParam.classSimpleName} set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #else
    public void set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #end
        this.${field.propertyName} = ${field.propertyName};
    #if(${chainSetter})
        return this;
    #end
    }
#end
#end
#end
## --基础getter/setter end---
## --额外字段getter/setter start--
#if(!${lombok})
    
    public Long get${pageCapitalName}() {
        return this.${pageParamName};
    }

    #if(${chainSetter})
    public ${queryParam.classSimpleName} set${pageCapitalName}(Long ${pageParamName}) {
    #else
    public void set${pageCapitalName}(Long ${pageParamName}) {
    #end
        this.${pageParamName} = ${pageParamName};
    #if(${chainSetter})
        return this;
    #end
    }

    public Long get${sizeCapitalName}() {
        return this.${sizeParamName};
    }

    #if(${chainSetter})
    public ${queryParam.classSimpleName} set${sizeCapitalName}(Long ${sizeParamName}) {
    #else
    public void set${sizeCapitalName}(Long ${sizeParamName}) {
    #end
        this.${sizeParamName} = ${sizeParamName};
    #if(${chainSetter})
        return this;
    #end
    }
#foreach($field in ${table.extraFields})

    #if(${field.propertyType.equals("boolean")})
        #set($getprefix="is")
    #else
        #set($getprefix="get")
    #end
    public ${field.propertyType} ${getprefix}${field.capitalName}() {
        return ${field.propertyName};
    }

    #if(${chainSetter})
    public ${queryParam.classSimpleName} set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #else
    public void set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #end
        this.${field.propertyName} = ${field.propertyName};
    #if(${chainSetter})
        return this;
    #end
    }
#end
#end
#if(!${lombok} && ${toString})

    @Override
    public String toString() {
        return "${queryParam.classSimpleName}{" +
    #foreach($field in ${table.fields})
        #if(${field.logicDeleteField})
        #else
            #if($!{foreach.index}==0)
        "${field.propertyName} = " + ${field.propertyName} +
            #else
        ", ${field.propertyName} = " + ${field.propertyName} +
            #end
        #end
    #end
    #foreach($field in ${table.extraFields})
        ", ${field.propertyName} = " + ${field.propertyName} +
    #end
        "}";
    }
#end
}
