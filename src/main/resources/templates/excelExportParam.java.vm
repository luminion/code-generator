package ${excelExportParam.packageName};
#if($excelExportParamFramePkg.size()>0)

#end
#foreach($pkg in ${excelExportParamFramePkg})
import ${pkg};
#end
#if($excelExportParamFramePkg.size()>0)

#end
#foreach($pkg in ${excelExportParamJavaPkg})
import ${pkg};
#end

/**
 * $!{table.comment}-Excel导出对象
 *
 * @author ${author}
#if(${javadocWithSeeTags})
 * @see ${entity.classCanonicalName}
 * @see ${serviceImpl.classCanonicalName}
#end
#if(${date})
 * @since ${date}    
#end
 */
#if(${lombok})
@Getter
@Setter
#end
#if(${lombok} && ${chainSetter})
@Accessors(chain = true)
#end
#if(${lombok} && ${toString})
@ToString
#end
@ExcelIgnoreUnannotated
public class ${excelExportParam.classSimpleName}#if(${serializable}) implements Serializable#end {
#if(${serializable})

    #if(${serializableAnnotation})
    @Serial
    #end
    private static final long serialVersionUID = 1L;
#end
## ----------  BEGIN 字段循环遍历  ----------
#foreach($field in ${table.fields})
#if(${field.logicDeleteField})
#else

##    #if("$!field.comment" != "")
##        #if(${springDoc})
##    @Schema(description = "${field.comment}")
##        #elseif(${swagger})
##    @ApiModelProperty("${field.comment}")
##        #else
##    /**
##     * ${field.comment}
##     */
##        #end
##    #end
    @ExcelProperty(value = "${field.comment}")
    private ${field.propertyType} ${field.propertyName};
#end
#end
## ----------  END 字段循环遍历  ----------
## ----------BEGIN getter/setter----------
#if(!${lombok})
#foreach($field in ${table.fields})
#if(${field.logicDeleteField})
#else
    #if(${field.propertyType.equals("boolean")})
        #set($getprefix="is")
    #else
        #set($getprefix="get")
    #end

    public ${field.propertyType} ${getprefix}${field.capitalName}() {
        return ${field.propertyName};
    }
    
    public void set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
        this.${field.propertyName} = ${field.propertyName};
    }
#end
#end
#end
## ----------  END getter/setter  ----------
#if(!${lombok} && ${toString})

    @Override
    public String toString() {
        return "${excelExportParam.classSimpleName}{" +
    #foreach($field in ${table.fields})
        #if(${field.logicDeleteField})
        #else
            #if($!{foreach.index}==0)
        "${field.propertyName} = " + ${field.propertyName} +
            #else
        ", ${field.propertyName} = " + ${field.propertyName} +
            #end
        #end
    #end
        "}";
    }
#end
}
