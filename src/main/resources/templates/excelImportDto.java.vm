package ${package.importDto};
#if($importDtoFramePkg.size()>0)

#end
#foreach($pkg in ${importDtoFramePkg})
import ${pkg};
#end
#if($importDtoJavaPkg.size()>0)

#end
#foreach($pkg in ${importDtoJavaPkg})
import ${pkg};
#end

/**
 * $!{table.comment}-Excel导入对象
 *
 * @author ${docAuthor}
#if(${docLink})
 * @see ${class.entity}
 * @see ${class.serviceImpl}
#end
#if(${docDate})
 * @since ${docDate}    
#end
 */
#if(${enableLombok})
@Getter
@Setter
#end
###if(${enableLombok} && ${enableChainSetter})
##@Accessors(chain = true)
###end
#if(${enableLombok} && ${enableToString})
@ToString
#end
@ExcelIgnoreUnannotated
public class ${excelImportParam.classSimpleName}#if(${enableSerializableUID}) implements Serializable#end {
#if(${enableSerializableUID})

    #if(${enableSerializableAnnotation})
    @Serial
    #end
    private static final long serialVersionUID = 1L;
#end
## ----------  BEGIN 字段循环遍历  ----------
#foreach($field in ${table.fields})
#if(${field.keyFlag})
#elseif(${field.versionField})
#elseif(${field.logicDeleteField})
#elseif(${field.fill} && (${field.fill}=="INSERT" || ${field.fill}=="INSERT_UPDATE"))
#elseif(${commandExcludeColumns.contains(${field.columnName})})
#else

##    #if("$!field.comment" != "")
##        #if(${enableOpenApi3})
##    @Schema(description = "${field.comment}")
##        #elseif(${enableOpenApi2})
##    @ApiModelProperty("${field.comment}")
##        #else
##    /**
##     * ${field.comment}
##     */
##        #end
##    #end
    @ExcelProperty(value = "${field.comment}")
    private ${field.propertyType} ${field.propertyName};
#end
#end
## ----------  END 字段循环遍历  ----------
## ----------BEGIN getter/setter----------
#if(!${enableLombok})
#foreach($field in ${table.fields})
#if(${field.keyFlag})
#elseif(${field.versionField})
#elseif(${field.logicDeleteField})
#elseif(${field.fill} && (${field.fill}=="INSERT" || ${field.fill}=="INSERT_UPDATE"))
#elseif(${commandExcludeColumns.contains(${field.columnName})})
#else
    #if(${field.propertyType.equals("boolean")})
        #set($getprefix="is")
    #else
        #set($getprefix="get")
    #end

    public ${field.propertyType} ${getprefix}${field.capitalName}() {
        return ${field.propertyName};
    }

    public void set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
        this.${field.propertyName} = ${field.propertyName};
    }
#end
#end
#end
## ----------  END getter/setter  ----------
#if(!${enableLombok} && ${enableToString})

    @Override
    public String toString() {
        return "${excelImportParam.classSimpleName}{" +
    #foreach($field in ${table.fields})
        #if(${field.keyFlag})
        #elseif(${field.versionField})
        #elseif(${field.logicDeleteField})
        #elseif(${field.fill} && (${field.fill}=="INSERT" || ${field.fill}=="INSERT_UPDATE"))
        #elseif(${commandExcludeColumns.contains(${field.columnName})})
        #else
            #if($!{foreach.index}==0)
        "${field.propertyName} = " + ${field.propertyName} +
            #else
        ", ${field.propertyName} = " + ${field.propertyName} +
            #end
        #end
    #end
        "}";
    }
#end
}
