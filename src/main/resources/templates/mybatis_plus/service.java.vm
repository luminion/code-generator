package ${package.service};
#if($serviceFrameworkPkg.size()>0)

#end
#foreach($pkg in ${serviceFrameworkPkg})
import ${pkg};
#end
#if($serviceJavaPkg.size()>0)

#end
#foreach($pkg in ${serviceJavaPkg})
import ${pkg};
#end

/**
 * $!{table.comment} 服务类
 *
 * @author ${author}
 * @since ${date}
 */
public interface ${service} extends ${serviceSuperClass}<${entity}> {
    #if(${generateQuery})

    ${queryVO} voById(Serializable id);

    List<${queryVO}> voList(${queryDTO} queryDTO);

    IPage<${queryVO}> voPage(${queryDTO} queryDTO, long current, long size);
    #end
    #if(${generateCreate})

    Serializable createByDTO(${createDTO} createDTO);
    #end
    #if(${generateUpdate})

    boolean updateByDTO(${updateDTO} updateDTO);
    #end
    #if(${generateDelete})
    
    @Override
    boolean removeById(Serializable id);
    #end
    #if(${generateImport})

    void excelTemplate(OutputStream os);

    int excelImport(InputStream is);
    #end
    #if(${generateExport})

    void excelExport(${queryDTO} queryDTO, OutputStream os);
    #end
}

