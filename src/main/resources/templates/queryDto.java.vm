package ${template.queryParam.packageName};
#if($queryParamFramePkg.size()>0)

#end
#foreach($pkg in ${queryParamFramePkg})
import ${pkg};
#end
#if($queryParamJavaPkg.size()>0)

#end
#foreach($pkg in ${queryParamJavaPkg})
import ${pkg};
#end

/**
 * $!{table.comment}-查询条件对象
 *
 * @author ${docAuthor}
#if(${docLink})
 * @see ${class.entity}
 * @see ${class.mapper}
 * @see ${class.serviceImpl}
 * @date ${docDate}
#end
 */
#if(${enableLombok})
@Getter
@Setter
#end
#if(${enableLombok} && ${enableChainSetter})
@Accessors(chain = true)
#end
#if(${enableLombok} && ${enableToString})
@ToString
#end
#if(${enableOpenApi3})
@Schema(description = "$!{table.comment}-查询条件对象")
#elseif(${enableOpenApi2})
@ApiModel(description = "$!{table.comment}-查询条件对象")
#end
public class ${queryParam.classSimpleName} #if(${queryParamSuperClass})extends ${queryParamSuperClass} #end#if(${enableSerializableUID})implements Serializable#end{
#if(${enableSerializableUID})

    #if(${enableSerializableAnnotation})
    @Serial
    #end
    private static final long serialVersionUID = 1L;
#end
#if(!${queryParamSuperClass})

    #if(${enableOpenApi3})
    @Schema(description = "当前页码")
    #elseif(${enableOpenApi2})
    @ApiModelProperty("当前页码")
    #else
    /**
     * 当前页码
     */
    #end
    private Long ${pageName};
    #if(${enableOpenApi3})
    @Schema(description = "每页条数")
    #elseif(${enableOpenApi2})
    @ApiModelProperty("每页条数")
    #else
    /**
     * 每页条数
     */
    #end
    private Long ${sizeName};
#end
## ----------  BEGIN 字段循环遍历  ----------
#foreach($field in ${table.fields})
#if(${field.logicDeleteField})
#else

    #if("$!field.comment" != "")
        #if(${enableOpenApi3})
    @Schema(description = "${field.comment}")
        #elseif(${enableOpenApi2})
    @ApiModelProperty("${field.comment}")
        #else
    /**
     * ${field.comment}
     */
        #end
    #end
    private ${field.propertyType} ${field.propertyName};
#end
#end
## ----------  END 字段循环遍历  ----------
#foreach($field in ${table.extraFields})

    #if("$!field.comment" != "")
        #if(${enableOpenApi3})
    @Schema(description = "${field.comment}")
        #elseif(${enableOpenApi2})
    @ApiModelProperty("${field.comment}")
        #else
    /**
     * ${field.comment}
     */
        #end
    #end
    private ${field.propertyType} ${field.propertyName};
#end
## --基础getter/setter start--
#foreach($field in ${table.fields})
#if(${field.logicDeleteField})
#else

    #if(${field.propertyType.equals("boolean")})
        #set($getprefix="is")
    #else
        #set($getprefix="get")
    #end
    public ${field.propertyType} ${getprefix}${field.capitalName}() {
        return ${field.propertyName};
    }

    #if(${enableChainSetter})
    public ${queryParam.classSimpleName} set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #else
    public void set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #end
        this.${field.propertyName} = ${field.propertyName};
    #if(${enableChainSetter})
        return this;
    #end
    }
#end
#end
## --基础getter/setter end---
## --额外字段getter/setter start--
#if(!${enableLombok})
#foreach($field in ${table.extraFields})

    #if(${field.propertyType.equals("boolean")})
        #set($getprefix="is")
    #else
        #set($getprefix="get")
    #end
    public ${field.propertyType} ${getprefix}${field.capitalName}() {
        return ${field.propertyName};
    }

    #if(${enableChainSetter})
    public ${queryParam.classSimpleName} set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #else
    public void set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #end
        this.${field.propertyName} = ${field.propertyName};
    #if(${enableChainSetter})
        return this;
    #end
    }
#end
#end
#if(!${enableLombok} && ${enableToString})

    @Override
    public String toString() {
        return "${queryParam.classSimpleName}{" +
    #foreach($field in ${table.fields})
        #if(${field.logicDeleteField})
        #else
            #if($!{foreach.index}==0)
        "${field.propertyName} = " + ${field.propertyName} +
            #else
        ", ${field.propertyName} = " + ${field.propertyName} +
            #end
        #end
    #end
    #foreach($field in ${table.extraFields})
        ", ${field.propertyName} = " + ${field.propertyName} +
    #end
        "}";
    }
#end
}
