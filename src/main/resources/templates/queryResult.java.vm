package ${queryResult.packageName};
#if($queryResultFramePkg.size()>0)

#end
#foreach($pkg in ${queryResultFramePkg})
import ${pkg};
#end
#if($queryResultJavaPkg.size()>0)

#end
#foreach($pkg in ${queryResultJavaPkg})
import ${pkg};
#end

/**
 * $!{table.comment}-查询返回对象
 *
 * @author ${docAuthor}
#if(${docLink})
 * @see ${entity.classCanonicalName}
 * @see ${mapper.classCanonicalName}
 * @see ${serviceImpl.classCanonicalName}
#end
#if(${docDate})
 * @since ${docDate}    
#end
 */
#if(${lombok})
@Getter
@Setter
#end
#if(${lombok} && ${chainSetter})
@Accessors(chain = true)
#end
#if(${lombok} && ${toString})
@ToString
#end
#if(${springDoc})
@Schema(description = "$!{table.comment}-VO")
#elseif(${swagger})
@ApiModel(description = "$!{table.comment}-VO")
#end
public class ${queryResult} #if(${queryResultSuperClass})extends ${queryResultSuperClass}#end#if(${serializable}) implements Serializable#end {
#if(${serializable})

    #if(${serializableAnnotation})
    @Serial
    #end
    private static final long serialVersionUID = 1L;
#end
## ----------  BEGIN 字段循环遍历  ----------
#if(!${queryResultExtendsEntity})
#foreach($field in ${table.fields})
#if(${field.logicDeleteField})
#else

    #if("$!field.comment" != "")
        #if(${springDoc})
    @Schema(description = "${field.comment}")
        #elseif(${swagger})
    @ApiModelProperty("${field.comment}")
        #else
    /**
     * ${field.comment}
     */
        #end
    #end
    private ${field.propertyType} ${field.propertyName};
#end
#end
#end
## ----------  END 字段循环遍历  ----------
#if(!${lombok} && !${queryResultExtendsEntity})
#foreach($field in ${table.fields})
#if(${field.logicDeleteField})
#else
    #if(${field.propertyType.equals("boolean")})
        #set($getprefix="is")
    #else
        #set($getprefix="get")
    #end

    public ${field.propertyType} ${getprefix}${field.capitalName}() {
        return ${field.propertyName};
    }

    #if(${chainSetter})
    public ${queryResult} set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #else
    public void set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #end
        this.${field.propertyName} = ${field.propertyName};
     #if(${chainSetter})
        return this;
    #end
    }
#end
#end
#end
#if(!${lombok} && ${toString})

    @Override
    public String toString() {
        return "${queryResult.classSimpleName}{" +
    #foreach($field in ${table.fields})
        #if(${field.logicDeleteField})
        #else
            #if($!{foreach.index}==0)
        "${field.propertyName} = " + ${field.propertyName} +
            #else
        ", ${field.propertyName} = " + ${field.propertyName} +
            #end
        #end
    #end
        "}";
    }
#end
}
